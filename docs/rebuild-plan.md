# Rebuild plan

The next implementation decision should select the TypeScript web/runtime architecture and document-rendering approach. Then introduce, in order: authentication and sessions, user-scoped CV/job records, DOCX extraction, factual tailoring plus change review, and DOCX/PDF export. Tailwind will establish the UI; use Tabulator for interactive tables and Highcharts for charts when those features are introduced.
