# Apache supplies these variables to the TypeScript service in deployment.
NODE_ENV=development
PORT=3000
DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=job_tune
DB_USER=job_tune
DB_PASSWORD=replace-with-a-local-secret
