{
  "name": "job-tune",
  "version": "0.1.0",
  "private": true,
  "description": "Job Tune: factual, AI-assisted CV tailoring.",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc",
    "check": "npm run typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
